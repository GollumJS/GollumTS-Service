language: node_js
node_js:
  - "node"
before_install:
  - npm i -g makeshift && makeshift -s @bcoe -r https://registry.npmjs.org
deploy:
  provider: npm
  email: $LOGN_PUBLISH
  api_key: $TOKEN_PUBLISH
  on:
    tags: true
